import COLORS from "../styles/theme";

const styles = {
    root: {
        backgroundColor: COLORS.white,
        overflowX: 'hidden',
        display: 'flex',
        minHeight: '99vh',
        height: '100%'
    },
    
    page: {
        backgroundColor: COLORS.white,
        overflowX: 'hidden',
        flex: 1
    }
};

export default styles;